# dependencies
node_modules/
.pnp.*

# env / secrets — NEVER commit these
.env
.env.*
!.env.example
secrets/

# build artifacts
dist/
build/
*.tsbuildinfo

# python solver (never commit the venv or caches)
solver/.venv/
__pycache__/
.pytest_cache/
*.pyc

# logs
*.log
npm-debug.log*

# os / editor
.DS_Store
.idea/
.vscode/
.cursor/

# next.js (apps added later)
.next/
out/

# external reference clones: cloned Arc/Circle sample repos that carry their own
# nested .git (would commit as broken submodules). Re-cloneable via context/README.md.
context/samples/

# original hackathon materials archive (binary)
Lepton.zip
